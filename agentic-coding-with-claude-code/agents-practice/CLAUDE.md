# CLAUDE.md

This directory (`agents-practice/`) is independent of its parent directory
(`agentic-coding-with-claude-code/`) and of that parent's other children (e.g. `notes/`). It is
used for independent practice and is tracked under the same git repository as the rest of the
workspace, but should not be assumed to share conventions, context, or state with sibling or
parent directories.
